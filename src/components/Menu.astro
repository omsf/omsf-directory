---
import AstroBubble from "./AstroBubble.astro";

interface Props {
  currentPage: string;
}
const { currentPage } = Astro.props;
---

<div class="my-4 flex flex-wrap justify-center gap-2">
  <AstroBubble tag="Software" select={currentPage === "software"} href="/" />
  <AstroBubble
    tag="Workflows"
    select={currentPage === "workflows"}
    href="/workflows"
  />
  <AstroBubble
    tag="Infrastructure"
    select={currentPage === "infrastructure"}
    href="/infrastructure"
  />
  <AstroBubble
    tag="Add your project!"
    select={currentPage === "new"}
    href="/new"
  />
</div>
