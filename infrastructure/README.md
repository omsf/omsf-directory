# Infrastructure entries

Add infrastructure entries as YAML files in this directory. Entries use the
same schema as software and workflow entries so they render with the shared
directory cards and filters.
